#ifndef COUNT_UNIQUE_MARKERS_H
#define COUNT_UNIQUE_MARKERS_H

#include <vector>
#include <algorithm>
#include "Marks.h"

Marks countUniqueMarkers(const std::vector<std::vector<int>>& matrix);

#endif
